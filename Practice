Pseudocode 1
Ask user for phrase
store that phrase in empty RESULT string
check each letter in the phrase once
  if character is lowercase fine its ASCII value
  add the changed character to the Result string
otherwise
  add the character as is to the RESULT string
Print the RESULT string after all characters are processed

Pseudocode 2
Ask user to enter first number
  store users first number
ask user to enter second number
  strore users second number
Add first number + second number
Get Result
Print the Result

Pseudocode 3
set total to 0
ask user to enter a number
  if user enters a letter show error message
add number to total
ask the user if they want to add more numbers
  if yes ask again to enter numbers
else
  if user sayes answers no stop ask for numbers
print the total

Pseudocode 4
ask user to enter a maximum whole number
  if user does not enter a whole number give error message
generate a random whole number betwwn 0
print the random whole number
ask the user if they want another random whole number
  if yes repeat asking for random wwhole numbers
else
  if user enters no Stop program

Pseudocode 5
set Left count to 0
set Right count to 0
repeat it 10 times
Genreate a random whole number grater that 0
if the number is divisible by 2 with no remainder print Right
increase Right count by 1
otherwise
print left
increase left count by 1
print how many times left occurred
print how many times Right occurred


